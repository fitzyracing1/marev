{
  "name": "marev-dex",
  "version": "1.0.0",
  "description": "MAREV DEX - Decentralized Exchange on Base",
  "main": "index.js",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network base",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "keywords": ["dex", "base", "ethereum"],
  "author": "fitzyracing1",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.20.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}
